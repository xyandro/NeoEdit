<Window Name="self" x:Class="NeoEdit.Disk.Dialogs.FindDialog" x:ClassModifier="internal"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:guiCommon="clr-namespace:NeoEdit.GUI.Common;assembly=NeoEdit.GUI"
	Title="Find By Expression" SizeToContent="WidthAndHeight" FocusManager.FocusedElement="{Binding ElementName=expression}">

	<Grid Margin="10">
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition Height="10"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<GroupBox Grid.Row="0" Grid.Column="0" Header="File Name">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition/>
					<ColumnDefinition/>
				</Grid.ColumnDefinitions>
				<Grid.RowDefinitions>
					<RowDefinition/>
					<RowDefinition/>
					<RowDefinition/>
				</Grid.RowDefinitions>

				<Label Grid.Row="0" Grid.Column="0" Content="_Name:" HorizontalAlignment="Right" Target="{Binding ElementName=expression}"/>
				<TextBox Name="expression" Grid.Row="0" Grid.Column="1" Width="400" Text="{Binding Expression, ElementName=self, UpdateSourceTrigger=PropertyChanged}"/>

				<CheckBox Grid.Row="1" Grid.Column="1" Content="_Regex" IsChecked="{Binding IsRegEx, ElementName=self}" Focusable="False"/>
				<CheckBox Grid.Row="2" Grid.Column="1" Content="_Full Path" IsChecked="{Binding FullPath, ElementName=self}" Focusable="False"/>
			</Grid>
		</GroupBox>

		<CheckBox Grid.Row="1" Grid.Column="1" Content="R_ecursive" IsChecked="{Binding Recursive, ElementName=self}" Focusable="False"/>

		<UniformGrid Grid.Row="3" Grid.Column="0" HorizontalAlignment="Right" Rows="1">
			<Button Click="OkClick" IsDefault="True" Content="Ok" Padding="10,1"/>
			<Button IsCancel="True" Content="Cancel" Padding="10,1"/>
		</UniformGrid>
	</Grid>
</Window>
