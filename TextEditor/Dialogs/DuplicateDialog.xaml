<Window Name="self" x:Class="NeoEdit.TextEditor.Dialogs.DuplicateDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:guiCommon="clr-namespace:NeoEdit.GUI.Common;assembly=GUI"
	Title="Duplicate selection" SizeToContent="WidthAndHeight" FocusManager.FocusedElement="{Binding ElementName=dupCount}">

	<StackPanel Margin="10,5">
		<Label Content="Duplicate how many times:" Target="{Binding ElementName=dupCount}"/>
		<StackPanel Orientation="Horizontal">
			<TextBox Name="dupCount" Margin="0,0,0,10" Width="500" Text="{Binding DupCount, ElementName=self}"/>
			<ScrollBar Name="spinner" SmallChange="1" Minimum="1" Maximum="1000000" Value="{Binding DupCount, ElementName=self}">
				<ScrollBar.RenderTransform>
					<TransformGroup>
						<ScaleTransform>
							<ScaleTransform.ScaleY>
								<MultiBinding Converter="{guiCommon:ExpressionConverter}" ConverterParameter="'0'-[0]/[1]">
									<Binding ElementName="dupCount" Path="ActualHeight"/>
									<Binding ElementName="spinner" Path="ActualHeight"/>
								</MultiBinding>
							</ScaleTransform.ScaleY>
						</ScaleTransform>
						<TranslateTransform Y="{Binding ActualHeight, ElementName=dupCount}"/>
					</TransformGroup>
				</ScrollBar.RenderTransform>
			</ScrollBar>
		</StackPanel>

		<UniformGrid Grid.Row="2" HorizontalAlignment="Right">
			<Button Click="OkClick" IsDefault="True" Content="Ok"/>
			<Button IsCancel="True" Content="Cancel"/>
		</UniformGrid>
	</StackPanel>
</Window>
