<Window Name="self" x:Class="NeoEdit.GUI.Dialogs.GotoIndexDialog" x:ClassModifier="internal"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:xctk="http://schemas.xceed.com/wpf/xaml/toolkit"
	Title="Go To Index" SizeToContent="WidthAndHeight" FocusManager.FocusedElement="{Binding ElementName=index}">

	<StackPanel Margin="10">
		<Label>
			<Label.Content>
				<TextBlock>
					<TextBlock.Text>
						<MultiBinding StringFormat="{}Go to index: ({0} - {1})">
							<Binding ElementName="self" Path="MinIndex"/>
							<Binding ElementName="self" Path="MaxIndex"/>
						</MultiBinding>
					</TextBlock.Text></TextBlock>
			</Label.Content>
		</Label>
		<xctk:IntegerUpDown Name="index" Width="300" Value="{Binding Index, ElementName=self}" Minimum="{Binding MinIndex, ElementName=self}" Maximum="{Binding MaxIndex, ElementName=self}"/>

		<CheckBox IsChecked="{Binding Relative, ElementName=self}" Content="_Relative" Focusable="False"/>

		<UniformGrid HorizontalAlignment="Right" Columns="2">
			<Button Click="OkClick" IsDefault="True" Content="Ok" Padding="10,1"/>
			<Button IsCancel="True" Content="Cancel" Padding="10,1"/>
		</UniformGrid>
	</StackPanel>
</Window>
