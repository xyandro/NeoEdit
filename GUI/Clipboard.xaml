<Window x:Name="self" x:Class="NeoEdit.GUI.Clipboard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Clipboard" Height="300" Width="300" FocusManager.FocusedElement="{Binding ElementName=items}">

	<ListBox Name="items" ItemsSource="{Binding Records, ElementName=self}" Height="Auto" />

</Window>
