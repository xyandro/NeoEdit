<guiControls:ModalDialog x:Name="self" x:Class="NeoEdit.GUI.Dialogs.HashDialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:guiControls="clr-namespace:NeoEdit.GUI.Controls"
	xmlns:guiConverters="clr-namespace:NeoEdit.GUI.Converters"
	Title="Choose Hash Algorithm" SizeToContent="WidthAndHeight" FocusManager.FocusedElement="{Binding ElementName=hashType}">

	<Grid Margin="10" Name="content">
		<Grid.Resources>
			<guiConverters:HexConverter x:Key="hexConverter"/>
		</Grid.Resources>

		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition Width="400"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition/>
			<RowDefinition Height="10"/>
			<RowDefinition/>
		</Grid.RowDefinitions>

		<Label Grid.Row="0" Grid.Column="0" Content="_Hash Algorithm:" Target="{Binding ElementName=hashType}" HorizontalAlignment="Right"/>
		<ComboBox Grid.Row="0" Grid.Column="1" Name="hashType" SelectedValue="{Binding HashType, ElementName=self}"/>

		<Label Grid.Row="1" Grid.Column="0" Content="_Key:" Target="{Binding ElementName=key}" HorizontalAlignment="Right"/>
		<TextBox Grid.Row="1" Grid.Column="1" Name="key" Text="{Binding Key, ElementName=self, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource hexConverter}}" IsEnabled="{Binding NeedsKey, ElementName=self}"/>

		<UniformGrid Grid.Row="3" Grid.ColumnSpan="2" HorizontalAlignment="Right" Rows="1">
			<Button Click="OkClick" IsDefault="True" Content="Ok" Padding="10,1"/>
			<Button IsCancel="True" Content="Cancel" Padding="10,1"/>
		</UniformGrid>
	</Grid>
</guiControls:ModalDialog>
