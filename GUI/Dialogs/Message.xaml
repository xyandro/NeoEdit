<common:TransparentWindow x:Name="self" x:Class="NeoEdit.GUI.Dialogs.Message"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:common="clr-namespace:NeoEdit.GUI.Common"
	Title="Message" Width="500" Height="90" SizeToContent="Height">

	<Grid Margin="10,5">
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>
		<TextBox BorderBrush="Transparent" TextWrapping="Wrap" Name="textBox" Text="{Binding Text, ElementName=self}" IsReadOnly="True"/>
		<UniformGrid Name="buttons" Grid.Row="1" MaxWidth="100">
			<UniformGrid.Resources>
				<Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
					<EventSetter Event="Click" Handler="ButtonHandler"/>
					<Setter Property="Margin" Value="3,0"/>
				</Style>
			</UniformGrid.Resources>
		</UniformGrid>
	</Grid>
</common:TransparentWindow>
