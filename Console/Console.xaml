<UserControl Name="self" x:Class="NeoEdit.Console.Console"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:guiCommon="clr-namespace:NeoEdit.GUI.Common;assembly=GUI"
	Background="White" Focusable="True" FocusVisualStyle="{x:Null}" FocusManager.FocusedElement="{Binding ElementName=command}">

	<Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<guiCommon:RenderCanvas x:Name="canvas" Grid.Row="0" Grid.Column="0" Background="Transparent" Margin="10,5" ClipToBounds="True"/>
		<ScrollBar Name="yScroll" Grid.Row="0" Grid.Column="1" Value="{Binding yScrollValue, ElementName=self}"/>

		<Grid Grid.Row="1" Grid.Column="0" Visibility="{Binding CommandMode, ElementName=self, Converter={guiCommon:ExpressionConverter}}">
			<Grid.ColumnDefinitions>
				<ColumnDefinition Width="Auto"/>
				<ColumnDefinition/>
			</Grid.ColumnDefinitions>
			<Grid.RowDefinitions>
				<RowDefinition/>
			</Grid.RowDefinitions>

			<Label Grid.Row="0" Grid.Column="0" Content="{Binding Location, ElementName=self}"/>
			<TextBox Grid.Row="0" Grid.Column="1" Name="command" Text="{Binding Command, ElementName=self}"/>
		</Grid>
	</Grid>

</UserControl>
