<controls:NEWindow x:Name="self" x:Class="NeoEdit.UI.Dialogs.File_Select_Choose_Dialog"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:controls="clr-namespace:NeoEdit.UI.Controls"
	Title="Active Tabs" SizeToContent="WidthAndHeight" FocusManager.FocusedElement="{Binding ElementName=files}">

	<Grid Margin="10">
		<Grid.ColumnDefinitions>
			<ColumnDefinition/>
			<ColumnDefinition Width="Auto"/>
		</Grid.ColumnDefinitions>
		<Grid.RowDefinitions>
			<RowDefinition/>
			<RowDefinition Height="10"/>
			<RowDefinition Height="Auto"/>
		</Grid.RowDefinitions>

		<ListView Grid.Row="0" Grid.Column="0" Name="files" Height="400" SelectionMode="Extended" SelectionChanged="OnFilesSelectionChanged">
			<ListView.View>
				<GridView>
					<GridViewColumn Header="Label" DisplayMemberBinding="{Binding Item1}" Width="500"/>
				</GridView>
			</ListView.View>
		</ListView>
		<StackPanel Grid.Row="0" Grid.Column="1">
			<Button Content="Move _Up" Click="OnFileMoveUpClick" IsEnabled="{Binding OneFileSelected, ElementName=self}"/>
			<Button Content="Move _Down" Click="OnFileMoveDownClick" IsEnabled="{Binding OneFileSelected, ElementName=self}"/>
			<Button Content="Move To _Top" Click="OnFileMoveToTopClick" Margin="0,10,0,0" IsEnabled="{Binding OneFileSelected, ElementName=self}"/>
			<Button Content="Move To _Bottom" Click="OnFileMoveToBottomClick" IsEnabled="{Binding OneFileSelected, ElementName=self}"/>
			<Button Content="_Close" Click="OnFileCloseClick" Margin="0,10,0,0"/>
		</StackPanel>

		<UniformGrid Grid.Row="2" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Right" Rows="1">
			<Button Click="OnOKClick" IsDefault="True" Content="Ok" Padding="10,1"/>
			<Button IsCancel="True" Content="Cancel" Padding="10,1"/>
		</UniformGrid>
	</Grid>

</controls:NEWindow>
